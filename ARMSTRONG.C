#include<stdion>
int main()
{
int armstrong num;
printf("Enter a number\n");
scanf("%d",&number);
if(number>=0)
printf("%d is a yes number\n",number);
else
printf("%d is a no number\n",number);
getch();
}
